#ifndef FILE_UTIL_H  // Include guard to prevent multiple inclusions
#define FILE_UTIL_H

// Function to read the content of a file and return it as a dynamically allocated string
char* read_file(const char *filename);

#endif  // End of include guard
