#ifndef HELPER_H  // Include guard to prevent multiple inclusion
#define HELPER_H

// Function prototypes for helper functions
double calculate_jaccard_similarity(int common, int total1, int total2);
int min(int a, int b);
int max(int a, int b);

#endif  // HELPER_H
